(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const api=async(action,query='')=>{const r=await fetch('/api/operations.js?action='+encodeURIComponent(action)+query,{cache:'no-store',credentials:'same-origin'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Erro ao carregar.');return d};
const injectTeamTab=()=>{if(location.pathname!=='/operador')return;const nav=document.querySelector('.operatorNav');if(!nav||nav.querySelector('a[href="/operador/equipe"]'))return;const a=document.createElement('a');a.href='/operador/equipe';a.textContent='Ver time';const cfg=nav.querySelector('a[href="/operador/configuracoes"]');cfg?nav.insertBefore(a,cfg):nav.appendChild(a)};
const mini=o=>`<a class="opsMini" href="/operador/equipe?operator=${encodeURIComponent(o.id)}"><img src="${esc(o.photo_url||'/logo.webp')}" alt=""><span><b>@${esc(o.nickname)}</b><small>${esc(o.rank||'Recruta')}</small></span></a>`;
async function roster(){if(location.pathname!=='/operador'||document.getElementById('opsRoster'))return;const host=document.querySelector('#app > section')||document.getElementById('app');if(!host)return;try{const d=await api('operator-rosters');const box=document.createElement('section');box.id='opsRoster';box.className='opsRoster card';box.innerHTML=`<div class="eyebrow">ESCALAÇÃO DOS JOGOS</div><h2>Quem vai, quem não vai e quem ainda não respondeu</h2>${(d.games||[]).map(g=>`<article class="opsRosterGame"><h3>${esc(g.title)}</h3><div class="opsRosterCols"><div class="opsRosterCol"><h4>✅ Vai (${g.going.length})</h4>${g.going.map(mini).join('')||'<p class="muted">Ninguém confirmou ainda.</p>'}</div><div class="opsRosterCol"><h4>❌ Não vai (${g.not_going.length})</h4>${g.not_going.map(mini).join('')||'<p class="muted">Ninguém marcou Não vou.</p>'}</div><div class="opsRosterCol"><h4>⏳ Ainda não respondeu (${g.pending.length})</h4>${g.pending.map(mini).join('')||'<p class="muted">Todos responderam.</p>'}</div></div></article>`).join('')||'<p class="muted">Nenhum jogo ativo.</p>'}`;host.appendChild(box)}catch(e){console.warn('Escalação',e)}}
async function teamPage(){if(location.pathname!=='/operador/equipe')return;const app=document.getElementById('app');if(!app||app.dataset.opsTeamLoaded==='1')return;app.dataset.opsTeamLoaded='1';try{const id=new URLSearchParams(location.search).get('operator');if(id){const d=await api('operator-profile','&id='+encodeURIComponent(id));const o=d.operator;app.innerHTML=`<section><div class="operatorNav"><a href="/operador">Visão geral</a><a class="active" href="/operador/equipe">Ver time</a><a href="/operador/configuracoes">Configurações</a></div><div class="card"><div class="eyebrow">PERFIL DO OPERADOR</div><div class="opsProfileHero"><img src="${esc(o.photo_url||'/logo.webp')}" alt="Foto"><div><h1>@${esc(o.nickname)}</h1><p><b>${esc(o.rank||'Recruta')}</b> · ${esc(o.function||'Operador')}</p><p>${esc(o.bio||'Sem descrição cadastrada.')}</p><p><b>Airsoft:</b> ${o.airsoft_years?esc(o.airsoft_years)+' anos':'Não informado'} · <b>Estilo:</b> ${esc(o.play_style||'Não informado')}</p><p><b>AEG principal:</b> ${esc(o.primary_replica||'Não informado')}<br><b>Secundária:</b> ${esc(o.secondary_replica||'Não informado')}</p></div></div><h3>Equipamentos</h3><div class="opsTeamGrid">${(d.equipment||[]).map(e=>`<div class="opsMemberCard">${e.photo_url?`<img src="${esc(e.photo_url)}" alt="">`:''}<div><strong>${esc(e.name)}</strong><small>${esc(e.category||'Equipamento')}</small><span>${esc(e.details||'')}</span></div></div>`).join('')||'<p class="muted">Nenhum equipamento cadastrado.</p>'}</div><h3>Galeria</h3><div class="opsMissionPhotos">${(d.gallery||[]).map(g=>`<img src="${esc(g.image_data)}" alt="${esc(g.caption||'Foto')}">`).join('')||'<p class="muted">Nenhuma foto cadastrada.</p>'}</div></div></section>`;return}
const d=await api('team-list');app.innerHTML=`<section><div class="operatorNav"><a href="/operador">Visão geral</a><a class="active" href="/operador/equipe">Ver time</a><a href="/operador/configuracoes">Configurações</a></div><div class="eyebrow">ÁREA DO OPERADOR</div><h1>Operadores ativos</h1><p class="muted">Clique em um operador para abrir o perfil completo.</p><div class="opsTeamGrid">${(d.operators||[]).map(o=>`<a class="opsMemberCard" href="/operador/equipe?operator=${encodeURIComponent(o.id)}"><img src="${esc(o.photo_url||'/logo.webp')}" alt=""><div><strong>@${esc(o.nickname)}</strong><span>${esc(o.name||'')}</span><small>${esc(o.rank||'Recruta')} · ${esc(o.function||'Operador')}</small></div></a>`).join('')}</div></section>`}catch(e){app.dataset.opsTeamLoaded='0';app.innerHTML=`<div class="error">${esc(e.message)}</div>`}}
function css(){if(document.getElementById('opsV2Css'))return;const s=document.createElement('style');s.id='opsV2Css';s.textContent=`.opsRoster{margin-top:24px}.opsRosterGame{margin:16px 0;padding:16px;border:1px solid #30363d;border-radius:12px}.opsRosterCols{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.opsRosterCol{padding:12px;border:1px solid #2d333b;border-radius:10px}.opsMini{display:flex;align-items:center;gap:8px;padding:6px 0;text-decoration:none}.opsMini img{width:36px;height:36px;border-radius:50%;object-fit:cover}.opsMini span{display:flex;flex-direction:column}.opsTeamGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.opsMemberCard{display:flex;gap:12px;align-items:center;padding:14px;border:1px solid #30363d;border-radius:12px;background:#111518;text-decoration:none}.opsMemberCard img{width:64px;height:64px;object-fit:cover;border-radius:12px}.opsMemberCard div{display:flex;flex-direction:column}.opsProfileHero{display:grid;grid-template-columns:150px 1fr;gap:20px}.opsProfileHero>img{width:150px;height:180px;object-fit:cover;border-radius:14px}.opsMissionPhotos{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.opsMissionPhotos img{width:100%;aspect-ratio:4/3;object-fit:cover;border-radius:10px}@media(max-width:760px){.opsRosterCols,.opsTeamGrid{grid-template-columns:1fr}.opsProfileHero{grid-template-columns:1fr}.opsProfileHero>img{width:120px;height:145px}.opsMissionPhotos{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s)}
function boot(){css();injectTeamTab();teamPage();roster()}
const obs=new MutationObserver(()=>{injectTeamTab();if(location.pathname==='/operador'&&!document.getElementById('opsRoster'))roster()});obs.observe(document.getElementById('app')||document.body,{childList:true,subtree:true});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,300));else setTimeout(boot,300);
setTimeout(boot,1200);setTimeout(boot,2500);
})();
