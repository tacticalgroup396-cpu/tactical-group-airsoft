(()=>{
  const path=location.pathname;
  const app=document.getElementById('app');
  if(!app)return;
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const apiMain=async(action,opts={})=>{const q=opts.query||'';const r=await fetch('/api/index.js?action='+encodeURIComponent(action)+q,{method:opts.method||'GET',headers:{'Content-Type':'application/json'},body:opts.body?JSON.stringify(opts.body):undefined,cache:'no-store',credentials:'same-origin'});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{d={error:'Resposta inválida do servidor.'}}if(!r.ok)throw new Error(d.error||'Erro ao carregar.');return d};
  const apiMission=async(action,opts={})=>{const q=opts.query||'';const r=await fetch('/api/mission?action='+encodeURIComponent(action)+q,{method:opts.method||'GET',headers:{'Content-Type':'application/json'},body:opts.body?JSON.stringify(opts.body):undefined,cache:'no-store',credentials:'same-origin'});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{d={error:'Resposta inválida do servidor.'}}if(!r.ok)throw new Error(d.error||'Erro ao carregar missão.');return d};
  const fmt=d=>{if(!d)return'';const s=String(d).slice(0,10),m=s.match(/^(\d{4})-(\d{2})-(\d{2})$/);return m?`${m[3]}/${m[2]}/${m[1]}`:s};
  const photo=o=>`<img class="tgaTeamPhoto" loading="lazy" decoding="async" src="${esc(o.photo_url||'/logo.webp')}" alt="Foto de ${esc(o.nickname||'operador')}">`;

  function addCss(){if(document.getElementById('tgaEnhanceCss'))return;const s=document.createElement('style');s.id='tgaEnhanceCss';s.textContent=`
  .tgaTeamGrid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}.tgaTeamCard{display:flex;gap:12px;align-items:center;padding:14px;border:1px solid #30363d;border-radius:15px;background:#111518;text-decoration:none;transition:.2s}.tgaTeamCard:hover{transform:translateY(-2px);border-color:#d9a326}.tgaTeamPhoto{width:62px;height:62px;border-radius:13px;object-fit:cover}.tgaTeamInfo{display:flex;flex-direction:column;min-width:0}.tgaTeamInfo small{opacity:.72}.tgaRoster{margin-top:24px}.tgaRosterCols{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.tgaRosterCol{padding:14px;border:1px solid #30363d;border-radius:14px;background:#0e1215}.tgaRosterMember{display:flex;gap:8px;align-items:center;padding:7px 0}.tgaRosterMember img{width:34px;height:34px;border-radius:50%;object-fit:cover}.tgaMissionCreate{margin-top:18px;padding-top:18px;border-top:1px solid #5b4919}.tgaMissionHead{padding:16px;border:1px solid #6b5520;border-radius:16px;background:radial-gradient(circle at 90% 0%,rgba(226,170,33,.20),transparent 35%),linear-gradient(145deg,#171711,#0d1012);margin-bottom:14px}.tgaMissionHead h3{margin:5px 0 4px}.tgaMissionGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.tgaMissionGrid textarea{min-height:92px}.tgaFull{grid-column:1/-1}.tgaMissionHub{margin-top:22px}.tgaMissionGameList{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.tgaMissionGameCard{border:1px solid #30363d;border-radius:15px;background:#111518;padding:15px}.tgaMissionGameCard.active{border-color:#d9a326;box-shadow:0 0 0 1px rgba(217,163,38,.18)}.tgaMissionEditor{margin-top:16px;padding:18px;border:1px solid #6b5520;border-radius:18px;background:linear-gradient(145deg,#15140e,#0c0f11)}.tgaMissionPeople{display:grid;gap:9px;margin-top:12px}.tgaMissionPerson{display:grid;grid-template-columns:minmax(180px,1.3fr) 1fr 1fr 90px 90px;gap:8px;align-items:center;padding:10px;border:1px solid #2d333b;border-radius:12px;background:#0e1114}.tgaPersonIdentity{display:flex;align-items:center;gap:9px}.tgaPersonIdentity img{width:40px;height:40px;border-radius:50%;object-fit:cover}.tgaMissionPhoto{max-width:360px;width:100%;border-radius:14px;margin-top:10px}.tgaMissionStatus{margin:10px 0;padding:10px 12px;border-radius:10px;background:#13171a;border:1px solid #30363d}.tgaMissionStatus.ok{border-color:#426f4b}.tgaMissionStatus.err{border-color:#7b3434;color:#ffb2b2}
  @media(max-width:900px){.tgaTeamGrid,.tgaMissionGameList{grid-template-columns:repeat(2,1fr)}.tgaMissionPerson{grid-template-columns:1fr 1fr}.tgaPersonIdentity{grid-column:1/-1}.tgaRosterCols{grid-template-columns:1fr}}
  @media(max-width:620px){.tgaTeamGrid,.tgaMissionGrid,.tgaMissionGameList{grid-template-columns:1fr}.tgaMissionPerson{grid-template-columns:1fr}.tgaPersonIdentity{grid-column:auto}}
  `;document.head.appendChild(s)}

  function operatorSubnav(active='team'){return `<div class="operatorNav"><a href="/operador">Visão geral</a><a class="${active==='team'?'active':''}" href="/operador/equipe">Ver operadores</a><a href="/operador/configuracoes">Configurações</a></div>`}
  function normalizeOperatorLinks(){if(!path.startsWith('/operador'))return;const nav=document.querySelector('.operatorNav');if(nav){let link=[...nav.querySelectorAll('a')].find(a=>/ver\s*(operadores|time)|equipe/i.test(a.textContent||''));if(!link){link=document.createElement('a');link.textContent='Ver operadores';const cfg=nav.querySelector('a[href="/operador/configuracoes"]');cfg?nav.insertBefore(link,cfg):nav.appendChild(link)}link.href='/operador/equipe';link.textContent='Ver operadores'}const header=[...document.querySelectorAll('header nav a')].find(a=>(a.textContent||'').trim()==='Equipe');if(header)header.href='/operador/equipe'}
  document.addEventListener('click',e=>{if(!path.startsWith('/operador'))return;const a=e.target.closest('a');if(!a)return;const txt=(a.textContent||'').trim().toLowerCase();if(txt==='ver operadores'||txt==='ver time'){e.preventDefault();e.stopPropagation();location.assign('/operador/equipe')}},true);

  async function renderOperatorTeam(){if(path!=='/operador/equipe')return;const id=new URLSearchParams(location.search).get('operator');try{if(id){const d=await apiMain('operator',{query:'&id='+encodeURIComponent(id)}),o=d.operator||{};app.innerHTML=`<section><div class="pageTitle"><div class="pageBrand">${photo(o)}<div><div class="eyebrow">PERFIL DO OPERADOR</div><h1>@${esc(o.nickname||'OPERADOR')}</h1><p>${esc(o.rank||'Recruta')} · ${esc(o.function||'Operador')}</p></div></div></div>${operatorSubnav('team')}<div class="card"><div style="display:flex;gap:18px;align-items:flex-start;flex-wrap:wrap">${photo(o)}<div><h2>${esc(o.name||o.nickname||'Operador')}</h2><p>${esc(o.bio||'Sem descrição cadastrada.')}</p><p><b>Patente:</b> ${esc(o.rank||'Recruta')} · <b>Função:</b> ${esc(o.function||'Operador')}</p><p><b>Estilo:</b> ${esc(o.play_style||'Não informado')} · <b>Airsoft:</b> ${esc(o.airsoft_years||'Não informado')}</p><p><b>Primária:</b> ${esc(o.primary_replica||'Não informado')}<br><b>Secundária:</b> ${esc(o.secondary_replica||'Não informado')}</p></div></div><h3>Equipamentos</h3><div class="tgaTeamGrid">${(d.equipment||[]).map(x=>`<article class="tgaTeamCard"><div class="tgaTeamInfo"><b>${esc(x.name)}</b><small>${esc(x.category||'Equipamento')}</small><span>${esc(x.details||'')}</span></div></article>`).join('')||'<p class="muted">Nenhum equipamento público cadastrado.</p>'}</div><div class="heroActions"><a class="outlinebtn" href="/operador/equipe">← Voltar para operadores</a></div></div></section>`}else{const d=await apiMain('team-members');app.innerHTML=`<section><div class="pageTitle"><div class="pageBrand"><img src="/logo.webp" alt="Tactical Group Airsoft"><div><div class="eyebrow">ÁREA DO OPERADOR</div><h1>Operadores do time</h1><p>Somente membros ativos. Abra o perfil de cada operador.</p></div></div></div>${operatorSubnav('team')}<div class="card"><div class="sectionHead compact"><div><div class="eyebrow">MEMBROS ATIVOS</div><h2>Equipe Tactical Group</h2></div><span class="tag">${(d.operators||[]).length} operador(es)</span></div><div class="tgaTeamGrid">${(d.operators||[]).map(o=>`<a class="tgaTeamCard" href="/operador/equipe?operator=${encodeURIComponent(o.id)}">${photo(o)}<div class="tgaTeamInfo"><b>@${esc(o.nickname)}</b><span>${esc(o.name||'')}</span><small>${esc(o.rank||'Recruta')} · ${esc(o.function||'Operador')}</small></div></a>`).join('')||'<p class="muted">Nenhum operador ativo.</p>'}</div></div></section>`}}catch(e){app.innerHTML=`<div class="error">${esc(e.message)}</div>`}}

  const rosterMember=o=>`<div class="tgaRosterMember"><img src="${esc(o.photo_url||'/logo.webp')}" alt=""><span><b>@${esc(o.nickname)}</b><small>${esc(o.rank||'')}</small></span></div>`;
  async function renderRoster(){if(path!=='/operador'||document.getElementById('tgaRoster'))return;try{const d=await apiMission('roster');const host=app.querySelector('section')||app;const box=document.createElement('section');box.id='tgaRoster';box.className='card tgaRoster';box.innerHTML=`<div class="eyebrow">ESCALAÇÃO DOS JOGOS</div><h2>Vai, não vai e ainda não respondeu</h2>${(d.games||[]).map(g=>`<div class="tgaMissionGameCard"><h3>${esc(g.title)}</h3><small>${fmt(g.game_date)}${g.game_time?' · '+esc(String(g.game_time).slice(0,5)):''}</small><div class="tgaRosterCols"><div class="tgaRosterCol"><b>✅ Vai (${g.going.length})</b>${g.going.map(rosterMember).join('')||'<p class="muted">Ninguém.</p>'}</div><div class="tgaRosterCol"><b>❌ Não vai (${g.not_going.length})</b>${g.not_going.map(rosterMember).join('')||'<p class="muted">Ninguém.</p>'}</div><div class="tgaRosterCol"><b>⏳ Não respondeu (${g.pending.length})</b>${g.pending.map(rosterMember).join('')||'<p class="muted">Todos responderam.</p>'}</div></div></div>`).join('')||'<p class="muted">Nenhum jogo ativo.</p>'}`;host.appendChild(box)}catch(e){console.warn('Escalação:',e)}}

  async function compressImage(file){if(!file)return'';const data=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file)});const img=await new Promise((resolve,reject)=>{const i=new Image();i.onload=()=>resolve(i);i.onerror=reject;i.src=data});let max=1200;const scale=Math.min(1,max/Math.max(img.width,img.height));const c=document.createElement('canvas');c.width=Math.round(img.width*scale);c.height=Math.round(img.height*scale);c.getContext('2d',{alpha:false}).drawImage(img,0,0,c.width,c.height);return c.toDataURL('image/jpeg',.72)}
  function missionCreateFields(){return `<div id="tgaMissionCreate" class="tgaMissionCreate"><div class="tgaMissionHead"><div class="eyebrow">MISSÃO DA PARTIDA</div><h3>Planejamento tático junto com o jogo</h3><p class="muted">Ao publicar, o jogo e a missão são criados juntos. Depois, somente quem marcar <b>Vou</b> poderá ser escalado nas equipes.</p></div><div class="tgaMissionGrid"><input name="mission_team_a" placeholder="Nome da Equipe A" value="Equipe A"><input name="mission_team_b" placeholder="Nome da Equipe B" value="Equipe B"><textarea class="tgaFull" name="mission_objective" placeholder="Objetivo principal da missão"></textarea><textarea name="mission_rules" placeholder="Regras / modo de jogo / condições da missão"></textarea><textarea name="respawn_rules" placeholder="Regras de respawn"></textarea><input name="mission_duration" placeholder="Duração da missão (ex.: 60 minutos)"><textarea name="secondary_objectives" placeholder="Objetivos secundários"></textarea><label class="tgaFull">Foto / mapa da missão<input name="mission_photo_file" type="file" accept="image/*"></label></div><div id="tgaMissionCreateStatus"></div></div>`}
  function enhanceGameForm(){if(path!=='/comandante/jogos')return;const form=document.getElementById('gameForm');if(!form||form.querySelector('#tgaMissionCreate'))return;const submit=[...form.querySelectorAll('button')].find(b=>/publicar jogo/i.test(b.textContent||''))||form.querySelector('button[type="submit"]');if(!submit)return;submit.insertAdjacentHTML('beforebegin',missionCreateFields())}

  document.addEventListener('submit',async e=>{if(path!=='/comandante/jogos'||e.target?.id!=='gameForm'||!e.target.querySelector('#tgaMissionCreate'))return;e.preventDefault();e.stopImmediatePropagation();const form=e.target,status=form.querySelector('#tgaMissionCreateStatus');const submit=form.querySelector('button[type="submit"],button.goldbtn');try{if(submit)submit.disabled=true;if(status)status.innerHTML='<div class="tgaMissionStatus">Criando jogo e missão...</div>';const fd=new FormData(form),file=fd.get('mission_photo_file');fd.delete('mission_photo_file');const payload=Object.fromEntries(fd.entries());const created=await apiMain('create-game',{method:'POST',body:payload});const gameId=created.game?.id;if(!gameId)throw new Error('O jogo foi criado sem retornar o identificador.');const missionPhoto=file instanceof File&&file.size?await compressImage(file):'';await apiMission('save',{method:'POST',body:{game_id:gameId,team_a_name:payload.mission_team_a||'Equipe A',team_b_name:payload.mission_team_b||'Equipe B',mission_objective:payload.mission_objective||'',mission_rules:payload.mission_rules||'',respawn_rules:payload.respawn_rules||'',mission_duration:payload.mission_duration||'',secondary_objectives:payload.secondary_objectives||'',mission_photo:missionPhoto,members:[]}});if(status)status.innerHTML='<div class="tgaMissionStatus ok">Jogo e missão criados com sucesso.</div>';setTimeout(()=>location.assign('/comandante/jogos?game='+encodeURIComponent(gameId)+'&missao=1'),350)}catch(err){if(status)status.innerHTML=`<div class="tgaMissionStatus err">${esc(err.message)}</div>`;if(submit)submit.disabled=false}},true);

  async function renderMissionHub(){if(path!=='/comandante/jogos'||document.getElementById('tgaMissionHub'))return;const form=document.getElementById('gameForm');if(!form)return;try{const d=await apiMission('games');const hub=document.createElement('section');hub.id='tgaMissionHub';hub.className='card tgaMissionHub';hub.innerHTML=`<div class="sectionHead compact"><div><div class="eyebrow">MISSÕES DOS JOGOS</div><h2>Planejamento e equipes</h2><p class="muted">Selecione uma partida para definir equipes, líder, médico, objetivo, vencedor, kills e mortes.</p></div></div><div class="tgaMissionGameList">${(d.games||[]).map(g=>`<article class="tgaMissionGameCard" data-mission-card="${g.id}"><div class="eyebrow">${g.mission_objective?'MISSÃO CONFIGURADA':'MISSÃO EM ABERTO'}</div><h3>${esc(g.title)}</h3><p>${fmt(g.game_date)} · ${esc(g.location||'')}</p><small>${Number(g.going_count||0)} confirmado(s) · ${Number(g.assigned_count||0)} escalado(s)</small><div class="heroActions"><button type="button" class="goldbtn small" data-open-mission="${g.id}">Configurar missão</button></div></article>`).join('')||'<p class="muted">Nenhum jogo ativo.</p>'}</div><div id="tgaMissionEditorHost"></div>`;const parent=form.closest('.adminGrid')||form.parentElement;parent.insertAdjacentElement('afterend',hub);hub.querySelectorAll('[data-open-mission]').forEach(b=>b.onclick=()=>openMissionEditor(b.dataset.openMission));const q=new URLSearchParams(location.search).get('game');if(q&&hub.querySelector(`[data-open-mission="${CSS.escape(q)}"]`))setTimeout(()=>openMissionEditor(q),80)}catch(e){console.warn('Missões:',e)}}

  async function openMissionEditor(gameId){const host=document.getElementById('tgaMissionEditorHost');if(!host)return;host.innerHTML='<div class="tgaMissionEditor">Carregando missão...</div>';try{const d=await apiMission('get',{query:'&game_id='+encodeURIComponent(gameId)}),m=d.mission||{},g=d.game||{},people=d.people||[];host.innerHTML=`<div class="tgaMissionEditor"><div class="tgaMissionHead"><div class="eyebrow">CENTRO DE OPERAÇÕES</div><h2>${esc(g.title)}</h2><p>${fmt(g.game_date)}${g.game_time?' · '+esc(String(g.game_time).slice(0,5)):''} · ${esc(g.location||'')}</p></div><form id="tgaMissionEditForm"><input type="hidden" name="game_id" value="${esc(gameId)}"><div class="tgaMissionGrid"><input name="team_a_name" value="${esc(m.team_a_name||'Equipe A')}" placeholder="Equipe A"><input name="team_b_name" value="${esc(m.team_b_name||'Equipe B')}" placeholder="Equipe B"><textarea class="tgaFull" name="mission_objective" placeholder="Objetivo principal">${esc(m.mission_objective||'')}</textarea><textarea name="mission_rules" placeholder="Regras / modo de jogo">${esc(m.mission_rules||'')}</textarea><textarea name="respawn_rules" placeholder="Respawn">${esc(m.respawn_rules||'')}</textarea><input name="mission_duration" value="${esc(m.mission_duration||'')}" placeholder="Duração"><textarea name="secondary_objectives" placeholder="Objetivos secundários">${esc(m.secondary_objectives||'')}</textarea><select name="winning_team"><option value="" ${!m.winning_team?'selected':''}>Vencedor ainda não definido</option><option value="A" ${m.winning_team==='A'?'selected':''}>Equipe A</option><option value="B" ${m.winning_team==='B'?'selected':''}>Equipe B</option><option value="Empate" ${m.winning_team==='Empate'?'selected':''}>Empate</option></select><label>Atualizar foto / mapa<input name="mission_photo_file" type="file" accept="image/*"></label></div>${m.mission_photo?`<img class="tgaMissionPhoto" src="${esc(m.mission_photo)}" alt="Foto da missão">`:''}<h3>Operadores confirmados no jogo</h3><p class="muted">Só aparecem aqui operadores que marcaram <b>Vou</b>.</p><div class="tgaMissionPeople">${people.map(o=>`<div class="tgaMissionPerson" data-mission-operator="${o.id}"><div class="tgaPersonIdentity">${photo(o)}<div><b>@${esc(o.nickname)}</b><small>${esc(o.rank||'')} · ${esc(o.function||'Operador')}</small></div></div><select data-team><option value="">Sem equipe</option><option value="A" ${o.team_code==='A'?'selected':''}>Equipe A</option><option value="B" ${o.team_code==='B'?'selected':''}>Equipe B</option><option value="RESERVE" ${o.team_code==='RESERVE'?'selected':''}>Reserva</option></select><select data-role><option value="operator" ${o.mission_role==='operator'?'selected':''}>Operador</option><option value="leader" ${o.mission_role==='leader'?'selected':''}>Líder</option><option value="medic" ${o.mission_role==='medic'?'selected':''}>Médico</option></select><input data-kills type="number" min="0" value="${Number(o.kills||0)}" title="Kills" placeholder="Kills"><input data-deaths type="number" min="0" value="${Number(o.deaths||0)}" title="Mortes" placeholder="Mortes"></div>`).join('')||'<p class="muted">Nenhum operador confirmou presença ainda.</p>'}</div><div id="tgaMissionEditStatus"></div><div class="heroActions"><button class="goldbtn" type="submit">Salvar missão</button></div></form></div>`;const edit=host.querySelector('#tgaMissionEditForm');edit.onsubmit=async ev=>{ev.preventDefault();const fd=new FormData(edit),file=fd.get('mission_photo_file');fd.delete('mission_photo_file');const base=Object.fromEntries(fd.entries()),members=[...edit.querySelectorAll('[data-mission-operator]')].map(row=>({operator_id:row.dataset.missionOperator,team_code:row.querySelector('[data-team]').value,mission_role:row.querySelector('[data-role]').value,kills:Number(row.querySelector('[data-kills]').value||0),deaths:Number(row.querySelector('[data-deaths]').value||0)}));const st=edit.querySelector('#tgaMissionEditStatus');try{st.innerHTML='<div class="tgaMissionStatus">Salvando...</div>';const img=file instanceof File&&file.size?await compressImage(file):'';await apiMission('save',{method:'POST',body:{...base,game_id:gameId,mission_photo:img,members}});st.innerHTML='<div class="tgaMissionStatus ok">Missão salva.</div>'}catch(err){st.innerHTML=`<div class="tgaMissionStatus err">${esc(err.message)}</div>`}};host.scrollIntoView({behavior:'smooth',block:'start'})}catch(e){host.innerHTML=`<div class="tgaMissionEditor"><div class="error">${esc(e.message)}</div></div>`}}

  addCss();
  if(path.startsWith('/operador')){
    normalizeOperatorLinks();
    const mo=new MutationObserver(()=>normalizeOperatorLinks());mo.observe(app,{childList:true,subtree:true});
    if(path==='/operador/equipe'){renderOperatorTeam();setTimeout(renderOperatorTeam,500)}
    if(path==='/operador'){setTimeout(renderRoster,900);setTimeout(renderRoster,2200)}
  }
  if(path==='/comandante/jogos'){
    const mo=new MutationObserver(()=>{enhanceGameForm();renderMissionHub()});mo.observe(app,{childList:true,subtree:true});
    enhanceGameForm();renderMissionHub();setTimeout(()=>{enhanceGameForm();renderMissionHub()},600);setTimeout(()=>{enhanceGameForm();renderMissionHub()},1600);
  }
})();
